# yaml-language-server: $schema=https://aka.ms/configuration-dsc-schema/0.2
# Laptop-only apps (4K-Laptop). Common apps are in winget-base.dsc.yaml.
# Add anything here that the laptop needs but the desktop doesn't.

properties:
  configurationVersion: 0.2.0
  resources: []
